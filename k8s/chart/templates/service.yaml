apiVersion: v1
kind: Service
metadata:
  name: {{ include "function-gemma-agent.fullname" . }}
  labels:
    {{- include "function-gemma-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "function-gemma-agent.selectorLabels" . | nindent 4 }}
